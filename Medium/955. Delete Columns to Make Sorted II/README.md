# 955. Delete Columns to Make Sorted II

**Difficulty:** Medium  
**Topics:** Strings, Greedy  

---

## Problem Description

You are given an array of `n` strings `strs`, all of the same length.

You may choose any **deletion indices**, and delete all the characters in those indices for each string.

For example, if `strs = ["abcdef","uvwxyz"]` and deletion indices `{0, 2, 3}`, then the final array after deletions is:

